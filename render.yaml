services:
  - type: web
    name: flask-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free  # これはあなたの計画に基づいて調整する (free, paid, etc.)
